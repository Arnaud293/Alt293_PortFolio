import React from 'react';
import Nav from '../components/Nav';

const Projects = () => {
    return (
        <div>
            <Nav />
            PROJECTS
        </div>
    );
};

export default Projects;